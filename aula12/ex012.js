var agr = new Date() //chama a função date
var hora = agr.getHours() // pega a hora atual do sistema
var min = agr.getMinutes() // pega os minutos atual do sistema
console.log(`agora são exatamente ${hora} horas e ${min}min`)